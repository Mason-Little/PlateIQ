{
  "name": "plateiq",
  "private": true,
  "workspaces": [
    "client",
    "server",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"bun --filter client run dev\" \"bun --filter server run start:dev\"",
    "build": "bun --filter @plateiq/entities run build && bun --filter client run build && bun --filter server run build",
    "lint": "bun --filter server run lint",
    "test": "bun --filter server run test",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "prettier": "^3.3.3"
  }
}
